<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewmodels="clr-namespace:SeeYouIn.ViewModels"
             x:Class="SeeYouIn.Views.AddReminderPage">
    <ContentPage.BindingContext>
        <viewmodels:AddReminderPageViewModel/>
    </ContentPage.BindingContext>
    <ContentPage.Content>
        <StackLayout>
            <Label Text="Add Reminder"/>
            <Entry Placeholder="Reminder Title" Text="{Binding ReminderTitle,Mode=TwoWay}"/>
            <Entry Placeholder="Reminder Body" Text="{Binding ReminderText,Mode=TwoWay}"/>
            <DatePicker Date="{Binding ReminderDate,Mode=TwoWay}"/>
            <Button Text="Add" Command="{Binding AddCommand}"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>